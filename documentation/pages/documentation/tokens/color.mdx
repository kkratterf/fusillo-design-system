import tokens from '../../../../design-tokens/export/tokens.js';

# 🎨 Color

## Light Mode

### Background

#### Neutral
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Background.Neutral).map(([tokenName, tokenInfo]) => (
      <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>

#### Brand
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Background.Brand).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Danger
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Background.Danger).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Warning
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Background.Warning).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Success
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Background.Success).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Info
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Background.Info).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Discovery
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Background.Discovery).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


### Text

#### Neutral
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Text.Neutral).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Brand
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Text.Brand).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Danger
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Text.Danger).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Warning
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Text.Warning).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Success
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Text.Success).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Info
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Text.Info).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Discovery
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Text.Discovery).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


### Border

#### Neutral
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Border.Neutral).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Brand
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Border.Brand).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Danger
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Border.Danger).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Warning
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Border.Warning).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Success
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Border.Success).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Info
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Border.Info).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Discovery
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Border.Discovery).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


### Icon

#### Neutral
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Icon.Neutral).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Brand
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Icon.Brand).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Danger
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Icon.Danger).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Warning
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Icon.Warning).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Success
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Icon.Success).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Info
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Icon.Info).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Discovery
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Light.Color.Icon.Discovery).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


## Dark Mode

### Background

#### Neutral
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Background.Neutral).map(([tokenName, tokenInfo]) => (
      <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>

#### Brand
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Background.Brand).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Danger
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Background.Danger).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Warning
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Background.Warning).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Success
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Background.Success).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Info
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Background.Info).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Discovery
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Background.Discovery).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


### Text

#### Neutral
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Text.Neutral).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Brand
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Text.Brand).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Danger
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Text.Danger).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Warning
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Text.Warning).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Success
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Text.Success).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Info
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Text.Info).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Discovery
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Text.Discovery).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


### Border

#### Neutral
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Border.Neutral).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Brand
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Border.Brand).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Danger
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Border.Danger).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Warning
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Border.Warning).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Success
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Border.Success).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Info
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Border.Info).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Discovery
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Border.Discovery).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


### Icon

#### Neutral
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Icon.Neutral).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Brand
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Icon.Brand).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Danger
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Icon.Danger).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Warning
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Icon.Warning).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Success
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Icon.Success).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Info
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Icon.Info).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>


#### Discovery
<br/>
{ <table className="w-full">
  <thead className="w-full">
    <tr className="border-0">
      <th className="border-t-0 border-x-0 px-2 py-1.5" align="left">Name and description</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Token</th>
      <th className="border-t-0 border-x-0 p-1.5" align="left">Color</th>
    </tr>
  </thead>
  <tbody className="w-full">
    {Object.entries(tokens.Tokens.Dark.Color.Icon.Discovery).map(([tokenName, tokenInfo]) => (
  <tr key={tokenName} className="w-full bg-transparent dark:bg-transparent even:bg-transparent even:dark:bg-transparent border-0">
    <td className="w-full font-mono font-medium border-t-0 border-x-0 pl-2 pr-3 py-3"><code>color-{tokenName}</code><div className="text-sm opacity-70 font-normal font-sans pl-0.5 pt-1.5">{tokenInfo.description}</div></td>
    <td className="border-t-0 border-x-0 p-1.5"><div className="w-40 opacity-70 font-medium font-mono text-sm"><code>color-{tokenInfo.original.value.split('.').pop().slice(0, -1)}</code></div></td>
    <td className="border-t-0 border-x-0 p-1.5">
      <div className="w-32 h-12 rounded-radius" style={{ backgroundColor: tokenInfo.value }}></div>
    </td>
  </tr>
))}
  </tbody>
</table> }
<br/>
