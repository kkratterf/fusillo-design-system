import { Tabs, Cards, Card } from 'nextra/components';
import { AlertPreview, AlertDefault, AlertDanger, AlertWarning, AlertSuccess, AlertInfo, AlertDiscovery } from '../../../components/components-alert.mdx'

# ❖ Alert

Displays a callout for user attention.

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <AlertPreview />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy 
    import { Alert, AlertIcon, AlertDescription, AlertTitle } from "@/components/alert"
    import { Megaphone } from "lucide-react" 

    export function AlertDemo() {
      return (
        <Alert>
          <AlertIcon>
            <Megaphone />
          </AlertIcon>
          <AlertTitle> Notice </AlertTitle>
          <AlertDescription>
            This is a general notification for your attention. It provides neutral information or basic reminders for everyday tasks.
          </AlertDescription>
        </Alert>
      )
    }
    ```
  </Tabs.Tab>
</Tabs>


## Usage

The alert component is designed to capture user attention quickly and deliver important information or feedback. The message within the alert should be concise and, if an action is required, it should provide clear guidance on what the user needs to do next. 

Alerts are typically used to inform users about a situation or a change in status that requires their immediate attention.

### When to use

- **To convey urgent information**: Use alerts to inform users of urgent and potentially disruptive issues that require immediate attention, such as error messages or warnings.
- **To disclose important changes**: If there's a significant change to the system or to the user's account, such as a system outage or a security breach, use an alert to ensure that the user is aware of the change.

Avoid overusing alerts for information that is not crucial or does not require immediate action, as this can lead to alert fatigue and reduce the effectiveness of the alerts.


## Examples

### Default
<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <AlertDefault />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy 
    import { Alert, AlertIcon, AlertDescription, AlertTitle } from "@/components/alert"
    import { Megaphone } from "lucide-react" 

    export function AlertDemo() {
      return (
        <Alert>
          <AlertIcon>
            <Megaphone />
          </AlertIcon>
          <AlertTitle> Notice </AlertTitle>
          <AlertDescription>
            This is a general notification for your attention. It provides neutral information or basic reminders for everyday tasks.
          </AlertDescription>
        </Alert>
      )
    }
    ```
  </Tabs.Tab>
</Tabs>

### Danger
<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <AlertDanger />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy 
    import { Alert, AlertIcon, AlertDescription, AlertTitle } from "@/components/alert"
    import { XCircle } from "lucide-react" 

    export function AlertDemo() {
      return (
        <Alert status="danger">
          <AlertIcon>
            <XCircle />
          </AlertIcon>
          <AlertTitle> Error Detected! </AlertTitle>
          <AlertDescription>
            Critical issue encountered! Please address this error immediately to prevent system failure or data loss.
          </AlertDescription>
        </Alert>
      )
    }
    ```
  </Tabs.Tab>
</Tabs>

### Warning
<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <AlertWarning />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy 
    import { Alert, AlertIcon, AlertDescription, AlertTitle } from "@/components/alert"
    import { AlertTriangle } from "lucide-react" 

    export function AlertDemo() {
      return (
        <Alert status="warning">
          <AlertIcon>
            <AlertTriangle />
          </AlertIcon>
          <AlertTitle> Caution Advised </AlertTitle>
          <AlertDescription>
            Be cautious! There are certain risks associated with this action. Review and proceed with care.
          </AlertDescription>
        </Alert>
      )
    }
    ```
  </Tabs.Tab>
</Tabs>

### Success
<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <AlertSuccess />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy 
    import { Alert, AlertIcon, AlertDescription, AlertTitle } from "@/components/alert"
    import { CheckCircle } from "lucide-react" 

    export function AlertDemo() {
      return (
        <Alert status="success">
          <AlertIcon>
            <CheckCircle />
          </AlertIcon>
          <AlertTitle> Operation Successful </AlertTitle>
          <AlertDescription>
            The action was completed successfully! Your changes have been saved or the process has finished without issues.
          </AlertDescription>
        </Alert>
      )
    }
    ```
  </Tabs.Tab>
</Tabs>

### Information
<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <AlertInfo />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy 
    import { Alert, AlertIcon, AlertDescription, AlertTitle } from "@/components/alert"
    import { Info } from "lucide-react" 

    export function AlertDemo() {
      return (
        <Alert status="info">
          <AlertIcon>
            <Info />
          </AlertIcon>
          <AlertTitle> Did You Know? </AlertTitle>
          <AlertDescription>
            Here's some useful information or context about the current process or section that might be helpful to you.
          </AlertDescription>
        </Alert>
      )
    }
    ```
  </Tabs.Tab>
</Tabs>

### Discovery
<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <AlertDiscovery />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy 
    import { Alert, AlertIcon, AlertDescription, AlertTitle } from "@/components/alert"
    import { BrainCircuit } from "lucide-react" 

    export function AlertDemo() {
      return (
        <Alert status="discovery">
          <AlertIcon>
            <BrainCircuit />
          </AlertIcon>
          <AlertTitle> Discover More </AlertTitle>
          <AlertDescription>
            Explore this feature! There are additional options and functionalities waiting to be uncovered that could enhance your experience.
          </AlertDescription>
        </Alert>
      )
    }
    ```
  </Tabs.Tab>
</Tabs>

## Useful links

**Here you go!** Here are all the resources you might need if you are a designer, a developer or a content manager:

<Cards className='mt-8'>
  <Card
    title="🤖 Storybook"
    href="https://main--653e613f4feb85c1b6467ba7.chromatic.com/?path=/docs/components-alert--documentation"
  />
  <Card
    title="🪄 Figma"
    href="https://www.figma.com/file/acdO58jx9zgGfkKu6htrx2/%F0%9F%94%B5-Fusillo-Design-System?type=design&node-id=233%3A751&mode=design&t=F5zgbD2phu8xvUpj-1"
  />
</Cards>  


## Accessibility

Thanks to the native [WAI-ARIA](https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Roles/alert_role) `alert role` the screen reader will be interrupted, announcing the new content immediately.


