import tokens from '../tokens/tokens.js';

export const TokensBorder = ({ type }) => (
  <>
    {
      <table>
        <thead>
          <tr>
            <th align="left">Token and description</th>
            <th align="right">
              <span className="pr-4">Value</span>
            </th>
            <th className="hidden sm:table-cell" align="left">
              Preview
            </th>
          </tr>
        </thead>
        <tbody>
          {Object.entries(tokens.Tokens.Light.Border[type]).map(
            ([tokenName, tokenInfo]) => (
              <tr key={tokenName}>
                <td className="w-full">
                  <div className="cell-tokens-name">
                    <code className="font-medium">{tokenName}</code>
                    <div className="cell-description-tokens">
                      {tokenInfo.description}
                    </div>
                  </div>
                </td>
                <td>
                  <div className="cell-value-scale" align="right">
                    {tokenInfo.value}
                  </div>
                </td>
                <td className="hidden sm:table-cell">
                  <div
                    className={type === 'Radius'
                      ? 'cell-preview-border-radius'
                      : type === 'Width'
                      ? 'cell-preview-border-width'
                      : {}}
                    style={
                      type === 'Radius'
                      ? { borderRadius: tokenInfo.value }
                      : type === 'Width'
                      ? { borderWidth: tokenInfo.value, }
                      : {}
                    }
                  ></div>
                </td>
              </tr>
            )
          )}
        </tbody>
      </table>
    }
  </>
);
