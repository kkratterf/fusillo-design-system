import colors from '../tokens/foundations.js';

export const FoundationsColor = ({ color }) => (
  <>
    {
      <table>
        <thead>
          <tr>
            <th align="left">Name</th>
            <th align="left">Value</th>
            <th className="hidden md:table-cell" align="left">
              Token
            </th>
            <th align="left">Preview</th>
          </tr>
        </thead>
        <tbody>
          {Object.entries(colors.Foundations.Color[color]).map(
            ([colorName, colorInfo]) => (
              <tr key={colorName}>
                <td className="cell-title">{colorName}</td>
                <td>
                  <div className="cell-value">{colorInfo.value}</div>
                </td>
                <td className="hidden md:table-cell">
                  <div className="cell-token">
                    <code>color-{colorName}</code>
                  </div>
                </td>
                <td>
                  <div
                    className="cell-preview"
                    style={{ backgroundColor: colorInfo.value }}
                  ></div>
                </td>
              </tr>
            )
          )}
        </tbody>
      </table>
    }
  </>
);
