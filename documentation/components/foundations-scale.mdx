import scale from '../tokens/foundations.js';

export const FoundationsScale = () => (
  <>
    {
      <table>
        <thead>
          <tr>
            <th align="left">Name</th>
            <th align="right"><span className='pr-4'>Value</span></th>
            <th className="hidden sm:table-cell md:hidden lg:table-cell" align="left">
              Preview
            </th>
          </tr>
        </thead>
        <tbody>
          {Object.entries(scale.Foundations.Scale).map(
            ([tokenName, tokenInfo]) => (
              <tr key={tokenName}>
                <td className="cell-title">
                  Space-{tokenName}
                </td>
                <td>
                  <div className="cell-value-scale" align="right">{tokenInfo.value}</div>
                </td>
                <td className="hidden sm:table-cell md:hidden lg:table-cell">
                  <div
                    className="cell-preview-scale"
                    style={{ width: tokenInfo.value }}
                  ></div>
                </td>
              </tr>
            )
          )}
        </tbody>
      </table>
    }
  </>
);
